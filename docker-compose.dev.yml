# Dev override — utiliser avec : docker compose -f docker-compose.yml -f docker-compose.dev.yml up
# Lance uniquement postgres en Docker, API et frontend tournent en local (hot reload)

version: "3.9"

services:
  api:
    profiles: ["skip"]
  frontend:
    profiles: ["skip"]
